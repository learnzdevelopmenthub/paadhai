# Design — Issue #<number>: <title>

> **Source artifact** for the Paadhai SDLC pipeline. Architecture, data structures, decisions. Each design choice references the REQ-IDs from `requirements.md` it satisfies.

## Overview

<1–2 sentence summary of the technical approach.>

## Architecture

<Plain text or ASCII diagram showing the components, data flow, and integration points. Use language the reader can follow without prior context.>

```
<ASCII diagram or component description>
```

## Data Structures / API Contracts

<Code blocks defining types, schemas, request/response shapes. Use the project's primary language. Reference REQ-IDs that each structure supports.>

```typescript
// Example
interface User {
  id: string;
  email: string;
  // ...
}
```

## Flow

<Numbered list of steps describing how the feature operates end-to-end. Reference REQ-IDs at each step.>

1. <step> — satisfies REQ-1
2. <step> — satisfies REQ-2, REQ-3
3. <step>

## Key Design Decisions

For each significant decision, capture: chosen approach, why, alternatives considered.

### Decision 1: <short title>
**Chosen:** <approach>
**Why:** <reason — link to REQ-ID if applicable>
**Alternatives rejected:** <one line per alternative>

### Decision 2: <short title>
**Chosen:** <approach>
**Why:** <reason>
**Alternatives rejected:** <…>

<…add more as needed…>

## Security Considerations

<Threat surfaces, mitigations, security checklist. Reference REQ-IDs that capture security requirements.>

### Attack Surfaces
- <surface>: <risk level> — <description>

### Mitigations
- <mitigation>

### Security Checklist
- [ ] Input validated at all entry points
- [ ] No sensitive data in error messages or logs
- [ ] Authorization checked before data access
- [ ] <issue-specific items, referencing REQ-IDs>

## Error Handling Strategy (optional)

<How errors propagate, how they're surfaced to the user, what gets logged.>

## Testing Strategy Outline (optional)

<High-level note on how this will be tested. The full test plan is generated by `/paadhai:dev-test` and lives in `test-plan.md`.>
